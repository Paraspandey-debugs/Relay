// Package binding provides a mobile-friendly facade for Relay's backend.
//
// The API is intentionally JSON-based so it can be consumed by mobile shells
// (gomobile, JNI, Swift bridging, c-shared wrappers) with minimal type glue.
package binding
